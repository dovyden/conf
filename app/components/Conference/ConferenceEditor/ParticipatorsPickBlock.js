import React from 'react';
import Participator from './Participator';

export default function ParticipatorsPickBlock(props) {
    return (
        <div className={props.className}>
        {/* тут должен быть цикл, на основании которого 
            генерятся отображения людей, уже выбранных 
            для участия в совещании */}
        </div>
    );
}